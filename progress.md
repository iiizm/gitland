Original prompt: Start a new project from the current folder and represent GitHub as a medieval 3D land. Each repository should become a building, interactions should read as roads, repository metrics should influence building size, people, heat, and topic clusters, and the map should support aerial movement, scroll zoom, and up to 90 days of data. The requested visual direction is a medieval village and castle landscape, iterated with design, test, graphics optimization, and planning feedback.

## Progress

- Started from an empty folder after removing the previous project files.
- Collected sub-agent feedback for product planning, medieval visual design, UI, graphics optimization, and testing.
- Chosen MVP assumption: sample 90-day data now, replaceable GitHub ingestion later.
- Created a Vite/Three.js project with medieval GitHub-land data mapping, aerial camera controls, topic villages, castles/houses, roads, crowds, minimap, and test hooks.
- Ran smoke validation and the develop-web-game Playwright client. Current visual pass: 44 repos, 6 topic villages, 52 roads, 231 visible people, and mixed building types (10 castles, 2 guildhalls, 11 manors, 21 houses).
- Confirmed `npm run build` succeeds. Vite reports the expected Three.js bundle-size warning.
- Added `scripts/collect-github-data.js` and refreshed `src/github-data.json` from the live GitHub REST API on 2026-05-21T02:00:16.454Z. Current live-data pass: 46 repos, 6 topic villages, 54 roads, 228 visible people, and building mix of 15 castles, 10 manors, 21 houses.
- Upgraded the visual direction toward a premium fantasy strategy-map look: ACES tone mapping, gradient sky, clouds, higher quality fog/light/shadows, procedural terrain, cobblestone roads, stone/roof/timber textures, gabled roofs, doors, window frames, chimneys, market tents, denser trees, bushes, rocks, contact shadows, and clearer river/road layering.
- Verified after the visual upgrade with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5174`, and the develop-web-game Playwright screenshot client. Current performance snapshot is visually stable but still heavy: ~32 fps in headless capture, 2863 draw calls, 167592 triangles.
- Ran a second material-quality pass with all sub-agent feedback focused on realistic surfaces: higher-resolution procedural albedo/bump textures for ground, stone, roof, timber, cobbles, and plaza; softened building edges with rounded geometry; added roof ridges/eaves and dirt grounding patches so buildings sit more naturally in the terrain. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5174`, and the Playwright screenshot client. Current snapshot: ~33 fps in headless capture, 3018 draw calls, 318256 triangles.
- Ran a third "AAA-style texture detail" iteration with the revised agent mix: 3 designer agents, 1 graphics optimization agent, 1 test agent, and 1 product/data-readability agent. Added explicit facade overlays for stone/plaster weathering, roof tile overlays, recessed windows with frames/sills/muntins, more detailed doors, market counters/crates/flags, character legs/cloaks, meadow grass/flower details, softer procedural ground patches, and a distant ridge-style mountain backdrop replacing the toy-like cone skyline. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5174`, and Playwright screenshot capture. Current snapshot remains stable with real GitHub data: 46 repos, 54 roads, 228 people, no console/WebGL errors, ~34 fps in headless capture, 5348 draw calls, 491180 triangles.
- Ran a fourth world-structure iteration with all agents focused on castle hierarchy and city-like districts. Castles now expose three influence tiers: smaller fortresses, standard castles, and large citadels with outer courts, larger towers, gatehouse towers, upper keeps, spires, halls, chapel elements, and denser parapets. Topic districts now add merged internal street networks, relation-filtered repo lanes, city boundary posts, lamps, lot/front-yard patches, crates, and building rotation toward the district center so houses feel arranged around streets rather than scattered. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://127.0.0.1:5174`. Current live-data snapshot: 46 repos, 111 logical roads including 57 city roads, 228 people, castle tier mix of 8 tier-1 / 4 tier-2 / 3 tier-3, no console/WebGL errors, ~28 fps in headless capture, 5670 draw calls, 534756 triangles.
- Ran a fifth realism/spacing iteration with 4 designer agents and 2 graphics optimization agents. Expanded topic centers and repo placement radii so villages separate more clearly while buildings inside each village have broader street spacing. Added town boundary rails, larger lot offsets, agricultural field belts with hedges, wider terrain/mountain bounds, less toy-like tree silhouettes, darker rougher roads/stone, higher-frequency ground bump/detail, and a lighter fog/lighting balance. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://127.0.0.1:5174`. Current live-data snapshot: 46 repos, 111 logical roads including 57 city roads, 279 people, no console/WebGL errors, ~29 fps in headless capture, 3658 draw calls, 496422 triangles.
- Ran a sixth density/environment/nature iteration. Updated the GitHub collector default from 8 to 30 repos per topic and refreshed live data on 2026-05-21T05:02:42.692Z: 172 rendered repos representing 1,227 matching 90-day active GitHub search results across the six topic queries. Added `detailLevel` so high-impact repos remain full-detail buildings while lower-ranked repos render as instanced outpost houses. Moved the mountain ring behind the playable area, softened edge terrain rise, enlarged the sky dome, added a horizon ground ring and distant forest belt, and replaced dot-like trees with taller instanced broadleaf/conifer archetypes, branches, softer tree shadows, and clustered forest placement. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://127.0.0.1:5174`. Current snapshot: 172 repos, 369 logical roads including 189 city roads, 294 people, no console/WebGL errors, ~28 fps in headless capture, 4109 draw calls, 702100 triangles.
- Ran a seventh village-road/brightness iteration. Reworked road control points so only major castles/buildings get direct district spurs, starting from plaza-edge lane points instead of the exact village center; most repo buildings now attach through merged city alleys. City road paths now use two bent control points, more relation-based alley links, adaptive segments, and irregular boundary/lamp/lot placement to break radial symmetry. Brightened the scene with higher exposure, lighter fog/sky, stronger hemisphere/fill lighting, lighter terrain/stone/road materials, and softer overlay/shadow opacity. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://127.0.0.1:5174`. Current snapshot: 172 repos, 249 logical roads including 216 city roads, spokeRatio 0.145, no console/WebGL errors, ~29 fps in headless capture, 3863 draw calls, 669844 triangles.
- Ran an eighth daylight/color-grading iteration with six agent reviews focused on brightness without adding GPU-heavy postprocessing. Lifted terrain albedo/vertex colors so grass no longer darkens through texture multiplication, brightened stone/plaster/timber/roof/cobble materials, reduced facade/roof overlay and contact-shadow opacity, shifted sky/fog toward a clearer daytime palette, and lightened tree crown material plus instance colors so forests read as foliage instead of dark masses. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://127.0.0.1:5174`. Current 90-day snapshot: 172 repos representing 1,227 results, 249 logical roads including 216 city roads, spokeRatio 0.145, 294 people, no console/WebGL errors, ~29 fps in headless capture, 3863 draw calls, 669844 triangles.
- Ran a ninth data-explanation/topic-identity iteration with six agent reviews. Clarified the HUD copy so `172 / 1.2K` reads as representative rendered buildings versus candidate repositories, not all GitHub repositories. Confirmed the current data collector samples six focused high-star topic queries with `perTopic=30`, unauthenticated `eventPages=1`, `eventReposPerTopic=4`, and 24 event-derived repos plus 148 metadata-estimated repos. Removed visible district fence/hedge rings and shifted topic identity into subtle ground color washes, plaza tint, topic-tinted lots/fields/crates, topic road tinting through vertex colors, and architecture multipliers for wall tone, body proportions, roof pitch, and tower proportions. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://localhost:5174`. Current 90-day snapshot: 172 repos representing 1,227 focused candidates, 249 logical roads including 216 city roads, spokeRatio 0.145, 294 people, no console/WebGL errors, ~29 fps in headless capture, 3784 draw calls, 637386 triangles.
- Ran a tenth authenticated full-candidate data pass. Added safe `.env` loading to `scripts/collect-github-data.js`, expanded search pagination up to GitHub's 1,000-result search window per topic query, and processed repository event collection with bounded concurrency from `.env`. Refreshed `src/github-data.json` using the user's local token on 2026-05-21T06:43:22.224Z: 1,189 unique repos rendered from 1,227 focused 90-day query candidates, with 1,186 repos event-derived and only 3 falling back to metadata estimates after event collection failures. Adjusted large-dataset rendering so all repos remain buildings/density points while city roads and visible crowd markers are sampled around important/hot/evenly distributed repos to avoid a spiderweb map. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://localhost:5174`. Current snapshot: 1,189 repos, 332 logical roads including 298 city roads, spokeRatio 0.022, 309 people, no console/WebGL errors, ~28 fps in headless capture, 4304 draw calls, 959856 triangles.
- Reverted only the post-collection display-density sampling changes so the user can inspect the immediate full-candidate view while keeping the authenticated 90-day data intact. City roads now connect all repos in each topic again and every rendered repo gets at least one visible person marker. Verified with `npm run build`, Playwright screenshot capture, and `npm run smoke -- --url http://localhost:5174`. Current snapshot: 1,189 repos, 1,277 logical roads including 1,243 city roads, 1,189 people, no console/WebGL errors, ~28 fps in headless capture, 4304 draw calls, 1,352,816 triangles.
- Added a separate original 3D Building Lab page at `building-gallery.html` for selecting future GitLand building directions. The page presents 37 non-clone procedural fantasy building candidates across Royal Arena, Frontier, Adventure, Citadel, Guild, and Outpost families, with a filter rail, selected-variant detail card, turntable camera, and `window.render_game_to_text()`/`window.advanceTime(ms)` inspection hooks. Verified initial gallery capture on a fresh Vite server: 37 variants visible, selected `Crown Keep`, no console/asset/WebGL errors.
- Ran a game-inspired Building Lab expansion with six agent reviews. Confirmed exact commercial game-building replicas are not included and should not be copied; added 25 original variants across `Arena Battler`, `Hunter Frontier`, and `Ancient Adventure` families so the requested inspirations read through broad silhouette language instead. Added richer procedural detail helpers for framed windows, doors, roof ridges, eaves, trim bands, crates, barrels, lanterns, chimneys, shield crests, stone courses, family-tinted plinth details, and cached materials. Verified with `npm run build`, the develop-web-game Playwright capture for `/building-gallery.html`, and `npm run smoke -- --url http://127.0.0.1:5175`. Current Building Lab snapshot: 62 variants, 10 families, no console/asset/WebGL errors, 1684 draw calls, 349870 triangles, 294 materials.
- Ran a `building-test` import pass with six agent reviews. Mapped the broad Zelda-like request to the current `Ancient Adventure` category and kept commercial game names out of source/UI copy. Adapted 12 procedural building forms from `iiizm/building-test` into `src/building-test-imports.js`, grouped them as `Imported Test Lab`, preserved source metadata in `render_game_to_text()`, and added direct filtering through `/building-gallery.html?family=Imported%20Test%20Lab`. Verified with `npm run build`, the develop-web-game Playwright capture for the imported filter, and `npm run smoke -- --url http://127.0.0.1:5175`. Current imported-filter snapshot: 74 total variants, 12 visible imported variants, no console/asset/WebGL errors, 919 draw calls, 127696 triangles, 431 materials.
- Ran a full `building-test` import correction after a six-agent audit found that the source contains 96 `BUILDINGS` specs and the previous pass exposed only 12. Regenerated `src/building-test-imports.js` from the source inventory so all 96 imported buildings and source form builders are available in `Imported Test Lab`, replaced the manual 12-item gallery block with `BUILDING_TEST_VARIANTS`, deferred gallery initialization so automated screenshots capture the latest rendered state, and kept all source/UI copy in English. Verified with `npm run build`, the develop-web-game Playwright capture for `/building-gallery.html?family=Imported%20Test%20Lab`, and `npm run smoke -- --url http://localhost:5175`. Current imported-filter snapshot: 158 total variants, 96 visible imported variants, no console/asset/WebGL errors, 4146 draw calls, 539496 triangles.
- Added a Building Lab curation flow so the user can save preferred building candidates without applying them to the main GitLand world. Saved picks persist only in browser `localStorage` under `gitland:buildingLab:savedVariants`, appear with a saved state in the list and selected card, and can be reviewed through the `Saved Picks` filter. Verified with `npm run build`, the develop-web-game Playwright capture for the imported filter, direct in-app browser save/filter/unsave checks, and `npm run smoke -- --url http://127.0.0.1:5175`.
- Improved the Building Lab save affordance after user confusion. The save control now appears as a large primary `Save This Building` button directly under the selected building summary, changes to `Remove Saved Pick` after saving, and keeps the saved state chip immediately below it so the curation action is visually separated from previous/next/rotate controls.
- Adjusted Building Lab for narrow/half-width browser use. The selected building card now remains visible below 900px instead of being hidden, keeps the primary save button in the compact bottom card, and the 3D candidate layout now uses a count-based near-square grid instead of a fixed four-column strip.
- Added a Building Lab filter visibility toggle in the header. `Hide Filters` collapses the entire variant browser panel off-screen so the user can inspect the 3D buildings, and `Show Filters` restores the filter/list panel without changing the current selection.
- Confirmed the user's Chrome profile still contains 15 saved building pick ids under `gitland:buildingLab:savedVariants`. Added `village-styles.html`, a 3D Village Style Board page that uses those 15 picks as the source silhouette set for six district civilization kits. Each district shows four castle tiers and four house tiers in one board, preserving selected base silhouettes while scaling size and detail density.
- Reworked the Village Style Board after the castle tier distinction review. Six agent reviews converged on making Castle II-IV read as different silhouettes, so each district now uses a unique selected-pick Castle IV anchor and stronger district-specific castle signatures: AI portal pylons, Frontend craft gate banners, Infra signal gantries, Database vault rings, Mobile leaf canopies, and Game lava/horn crowns. Verified with `npm run build`, Korean-source scan, and the develop-web-game Playwright capture for `/village-styles.html`.
- Applied the Village Style Board kits to the main Kingdom Map. `buildSettlementStageImport()` now resolves each topic/type/stage through the selected-pick kit mapping, including custom settlement builders for `obsidian-bastion`, `rift-gate-citadel`, and `black-crown-keep`. Main-world `render_game_to_text()` now reports per-repo `settlementPickId`, `settlementSourceId`, `settlementKitSource`, and per-topic `villageKit` mappings. Verified with `npm run build`, Korean-source scan, and a Playwright render payload check on `/index.html`: all 15 selected pick IDs are actively rendered in the kingdom, while the remaining low-detail repos stay as instanced outposts for performance.
- Ran a civilization-architecture pass after the goal was tightened to require every building tier, from castles to houses and outposts, to read as a different species or clan by topic. Six agent reviews converged on reducing shared kit overlap, restoring castle/house stages 1-4 in the main world, keeping outposts instanced, and adding debug evidence for tier coverage. Reworked selected settlement kits so AI, Frontend, Infra, Database, Mobile, and Game use more distinct source families; added shared species architecture signatures and low-poly merged ornaments for full settlements; kept outposts on topic-specific instanced silhouettes; expanded labels, hover, selection, and `render_game_to_text()` with clan/tier/signature data; and strengthened smoke assertions for castle/house/outpost tier coverage and unique species signatures. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, and a render payload check: 48 full settlements, 1,141 outposts, no missing castle/house stages, no console/asset/WebGL errors, 2,907 draw calls, and 1,600,308 triangles.
- Ran a trend-readability pass with four designer agents, one graphics optimization agent, and one QA agent focused on making GitHub momentum readable from the villages and buildings themselves. The reviews converged on keeping new work out of heavy 3D geometry and using district signposts, minimap pings, hover scouting text, selection context, and structured debug payloads. Added per-window trend scoring, topic field ranks, field top repo markers, relative rising repo counts, global/topical repo ranks, dominant recent activity signals, and a `trend` digest in `render_game_to_text()`. District labels now show `#rank`, field abbreviation, Heat, top repo, repo count, and rising count; the minimap uses stronger heat washes, top-three pulse rings, and gold pings for top repos; hover/selection panels now put Field/District context before clan details. Strengthened smoke coverage so 90/30/7-day windows assert hot topic rank continuity, rendered top repo cross-references, hot repo activity breakdowns, selected repo trend fields, and trend coverage counts. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, and a Playwright render payload check: six visible district labels, no label clipping/overlap after edge clamping, 18 hot repo leaderboard entries, no console/asset/WebGL errors, 2,907 draw calls, and 1,600,308 triangles.
- Ran a background/landscape quality pass with four landscape designers, one graphics optimization agent, and one QA agent focused on making the scenery feel more like a large polished game world without breaking the web budget. The reviews converged on low-cost atmospheric layering and water-edge realism instead of adding heavy geometry. Added a three-depth sky gradient, two horizon haze bands, warmer directional sun/cooler fill balance, a third blue far-mountain ridge, distance-tinted horizon forest bands, wet shoreline and sandbar rings around every lake, deeper central water ribbons in rivers/canals, a coastal crescent and inlet for Mobile Lagoon, plus instanced pebbles, driftwood, and foam flecks along water. Expanded `scenicFeatures` with composition, backgroundLayers, placementQuality, detailDensity, and landscapeBudget evidence, then strengthened smoke assertions for visible water coverage, layered background scenery, shoreline detail, placement quality, and scenic performance budget. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and screenshot review: 5 lakes, 4 water courses, 10 shore rings, 4 deep water ribbons, 90 pebbles, 20 driftwood pieces, 52 foam flecks, 558 instanced scenic props, visible water count 9, no console/asset/WebGL errors, 2,951 draw calls, and 1,616,792 triangles.
- Ran a label-independent trend-marker pass with four designer agents, one graphics optimization agent, and one QA agent focused on making top repos and hot fields readable from buildings and villages alone. The reviews converged on separating permanent topic/species identity from temporary trend light: village heat aura rings for each field, repo halo rings for topic top-three projects, and restrained gold/topic-colored beacons plus crown crystals for field-leading and global hot repos. Implemented a single `createTrendMarkers()` instanced layer tagged as `trendMarkers`, exposed `scene.trendVisuals`, `repos[].worldTrendMarker`, and `topicIdentity[].trendVisualIdentity`, and strengthened smoke assertions so 90/30/7-day windows require field auras, top repo building markers, global hot repo markers, label-independent flags, and a strict triangle budget. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and a label-hidden screenshot: 24 marked repos, 6 field heat villages, 18 global hot repo markers, 6 field top beacons, 30 ring instances, 19 beacon instances, 24 crown instances, trend marker budget 3,472 triangles / 4 draw calls, no console/asset/WebGL errors, 2,959 draw calls, and 1,623,736 triangles.
- Ran a performance headroom pass with two graphics optimization agents, two design preservation agents, one QA agent, and one worldbuilding agent focused on reducing draw calls without weakening topic/species identity or trend readability. The reviews converged on safe local batching before more visual detail. Replaced 96 individual full-settlement dirt patch/contact shadow meshes with two instanced ground-mark layers while leaving building silhouettes, species ornaments, outpost accents, trend halos, and scenery untouched. Added `performance.drawCallBreakdown` and `performance.optimization.buildingGroundMarks` evidence, then strengthened smoke assertions so 90/30/7-day windows require the two-mesh instanced ground mark path, 94 saved draw calls, style-preserving flags, and no cross-topic building geometry merge. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and a label-hidden screenshot: 48 dirt patch instances, 48 contact shadow instances, ground mark budget 3,456 triangles / 2 draw calls, no console/asset/WebGL errors, 2,879 draw calls, and 1,624,240 triangles.
- Ran a civilization landmark pass with four designers, one graphics optimization agent, and one QA agent focused on making each topic village readable as a different civilization from the horizon without confusing trend markers. The reviews converged on treating landmarks as permanent, physical topic identity and trend markers as temporary external light. Rebuilt `createDistrictLandmark()` into larger edge-anchored silhouettes: AI crystal research tower, Frontend market arcade gate, Infra pipe fortress watchtower, Database tiered vault archive, Mobile harbor mast lighthouse, and Game arena citadel gate. Added `civilizationLandmarks` render-category accounting, `scene.distantLandmarks`, and `topicIdentity[].distantLandmarkIdentity`, then strengthened smoke assertions so 90/30/7-day windows require six unique archetypes, unique silhouettes/material palettes, edge placement, no repo overlap, trend-coupled false, and small geometry/draw-call budgets. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and a label-hidden screenshot: 6 civilization landmarks, 6 unique silhouettes, 4,836 landmark triangles / 30 landmark draw calls, no landmark/repo overlap, no console/asset/WebGL errors, 2,821 draw calls, and 1,621,212 triangles.
- Ran an outpost and district-ground identity pass with four designer agents, one graphics optimization agent, and one QA agent focused on making topic villages more legible without adding more roads. The reviews converged on two changes: strengthen permanent village ground identity with edge bands and entrance aprons, and make the 1,141 outposts read as topic-specific terminal forms instead of one shared house mesh. Added `districtGroundIdentity` as a two-draw-call instanced ground layer with 12 civilization edge bands and 18 entrance aprons, added six outpost geometry families (`needle-spire`, `market-booth`, `pipe-yard`, `vault-pod`, `stilt-canopy`, and `arena-camp`) with separate body/roof/accent/ground-contact signatures, exposed `scene.outpostIdentity`, `scene.districtGroundIdentity`, `topicIdentity[].groundIdentity`, and per-repo outpost geometry signatures, and tightened smoke assertions for label-independent topic coverage, 7/30/90-day stability, strict draw-call/triangle budgets, and categorized render costs. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and a label-hidden screenshot: 6 unique outpost geometry families, 6 unique roof/accent/ground-contact families, 1,141 outpost identity instances, 59,706 outpost identity triangles / 18 draw calls, 2,880 district identity triangles / 2 draw calls, no console/asset/WebGL errors, 2,845 draw calls, and 1,495,376 triangles.
- Ran an open-world background vista pass with four world/design agents, one graphics optimization agent, and one QA agent focused on making the kingdom feel like part of a larger explorable world without expanding the playable bounds or adding heavy mesh detail. The reviews converged on a separately budgeted `backgroundVista` layer: broken outer cliff arcs, distant pass openings, flat plateau silhouettes, and low foothill transition patches behind the existing horizon forest. Added a static no-shadow/no-raycast `backgroundVista` render category with structured scenic payload (`distantCliffArcs`, `distantPassOpenings`, `distantPlateaus`, `foothillTransitionPatches`, radius range, silhouette signature, and render budgets), preserved it across 7/30/90-day time-window rebuilds, and strengthened smoke assertions for vista depth, strict draw-call/triangle limits, trend independence, and time-window stability. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and a label-hidden screenshot: 6 cliff arcs, 6 pass openings, 5 plateau silhouettes, 58 foothill patches, background vista budget 2,060 triangles / 3 draw calls, no console/asset/WebGL errors, 2,851 draw calls, and 1,499,496 triangles.
- Ran a trend-causality readability pass with four design agents, one graphics optimization agent, and one QA agent focused on making hot fields and top repos understandable from the world geometry itself. The reviews converged on adding temporary trend light, not permanent civilization geometry: one low ground-flow ribbon from each village heat aura to that field's current top repo, plus a small visible dominant-signal glyph above every marked repo crown. Kept both sublayers inside `trendMarkers` with shared vertex-colored materials, no shadows, no raycast targets, and explicit payload evidence for flow anchors, causal top-repo links, signal glyph families, and strict budgets. Strengthened smoke assertions so every 90/30/7-day window must show six field-to-top-repo heat flows, dominant-signal glyphs matching repo activity causes, and trend render cost under 6 draw calls / 6,000 triangles.
- Ran a road/render headroom pass with two graphics optimization agents, two design-preservation agents, one QA agent, and one road optimization reviewer. The reviews warned against global full-settlement/outpost merges that could weaken village species identity, so the implemented pass targeted non-identity render overhead: major inter-district roads and landmark spurs now render as one merged edge/top pair while preserving logical road counts and topic-tinted vertex colors, local city roads use the same batching path, and full-settlement hit proxy meshes are hidden from the render pass while remaining raycastable for selection. Added `performance.optimization.roadRibbonBatching` and `fullSettlementHitProxies` evidence, expanded smoke coverage for logical road counts, city road visibility semantics, hidden-hitbox selection, and permanent identity signatures across time windows, and verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, payload inspection, and screenshot review. Current snapshot: 139 logical roads preserved, road draw calls reduced to 4, full-settlement draw calls reduced to 846, total draw calls 2,645, 1,498,624 triangles, no console/asset/WebGL errors.
- Ran a full-settlement decor batching pass with two graphics optimization agents, two design-preservation agents, one QA agent, and one visual QA reviewer. The reviews pushed for batching only static opaque settlement decoration while preserving topic, castle/house type, clan, and species architecture signatures. Added `createFullSettlementDecorBatches()` as a topic/type/species/material scoped merge pass after full settlements are built, exposed `performance.optimization.fullSettlementDecorBatches` evidence, and strengthened smoke coverage so every 90/30/7-day window asserts complete topic/tier/species coverage, no cross-topic/type/species merges, hidden hit-proxy preservation, and categorized triangle/draw-call accounting. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, payload inspection, and screenshot review. Current snapshot: 48 full settlements, 813 eligible decor candidates, 477 source meshes merged into 170 decor batches, 307 draw calls saved, full-settlement base draw calls reduced to 369, total draw calls 2,175, 1,521,404 triangles, no console/asset/WebGL errors.
- Ran a full-settlement species glyph pass with four design/readability agents, one graphics optimization agent, and one QA agent. The reviews converged on giving every full castle and house a fixed clan crest/plaque so village species identity is visible from buildings themselves, while keeping new geometry categorized and instanced instead of falling into `other`. Added topic-specific instanced `fullSettlementSpeciesGlyphs` for AI crystal sigils, Frontend banner panels, Infra service gantries, Database vault rings, Mobile sail masts, and Game horn emblems; exposed `scene.fullSettlementGlyphIdentity`; expanded per-repo settlement visual signatures with source form/surface/palette/designer and glyph family; and strengthened `scene.visualTierMatrix` plus smoke coverage so all 48 topic+tier cells are distinct and stable across 90/30/7-day windows. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, payload inspection, and screenshot review. Current snapshot: 48 full-settlement glyph instances in 6 topic-instanced meshes, 6 unique glyph families, all castle/house tiers covered, 48 unique topic+tier visual signatures, glyph budget 5,552 triangles / 6 draw calls, `other` unchanged at 379 draw calls, total draw calls 2,187, 1,532,508 triangles, no console/asset/WebGL errors.
- Ran a full-settlement species contact-court pass with four designer agents, one graphics optimization agent, and one QA/test agent. The reviews converged on making the new layer a permanent building-footprint identity treatment, not more roads or trend glow: AI gets cold rune-node paving, Frontend shopfront mats, Infra heavy service-yard plates, Database vault rings, Mobile stilt-deck mats, and Game arena/camp rings. Replaced generic full-settlement dirt patches with one merged vertex-colored `fullSettlementContactCourts` mesh plus one instanced contact-shadow mesh, added contact court family/signature data to repos, topic identity, `scene.visualTierMatrix`, and `scene.fullSettlementContactCourtIdentity`, and separated it explicitly from `github-trend-signal` trend markers. Strengthened smoke coverage so 90/30/7-day windows assert 48 contact courts, six unique families, castle/house tier coverage, no road clutter, trend independence, categorized draw/triangle accounting, and stable contact-court signatures. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, payload inspection, and screenshot review. Current snapshot: 48 contact court instances, 6 unique contact court families, 48 unique topic/tier cells, contact court budget 3,336 triangles / 1 draw call plus 1 shadow draw call, `other` unchanged at 379 draw calls, total draw calls 2,188, 1,537,452 triangles, no console/asset/WebGL errors.
- Ran a district identity props pass with four designers, one graphics optimization agent, and one QA agent focused on making village entrances and plaza edges read as different topic civilizations without adding roads. The reviews converged on one permanent physical prop layer, separate from trend glow and separate from district ground decals. Added `districtIdentityProps` as a single baked vertex-colored mesh with 18 entrance anchors and 36 plaza-edge anchors: AI crystal/rune sentries, Frontend market banners/signboards, Infra pipe gantries/service boxes, Database archive tablets/ring steles, Mobile stilt/sail markers, and Game horn/shield standards. Exposed `scene.districtPropsIdentity` and per-topic `districtPropsIdentity`, added prop families into permanent topic identity signatures, and strengthened smoke coverage for six unique prop families, no trend inputs, no road/path meshes, time-window stability, categorized render accounting, and unchanged `other`. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, payload inspection, and screenshot review. Current snapshot: 54 district prop anchors, 234 logical prop pieces, 6 unique prop families, prop budget 2,523 triangles / 1 draw call, `other` unchanged at 379 draw calls, total draw calls 2,190, 1,542,498 triangles, no console/asset/WebGL errors.
- Ran a full-settlement roof/facade surface identity pass with four designer agents, one graphics optimization agent, and one QA/test agent focused on making the actual castle-to-house building envelopes read as different topic species. The design reviews converged on silhouette-first roofline families plus facade confirmation: AI crystal fins/rune panels, Frontend awning banners/shop signs, Infra pipe gantries/service plates, Database vault lids/archive tablets, Mobile sail masts/dock slats, and Game horn standards/shield trophies. Implemented one baked vertex-colored `fullSettlementSurfaceIdentity` mesh attached to all 48 full settlements, exposed roofline/facade/surface families in repo payloads, `speciesSignature`, `topicIdentity[].speciesArchitecture`, `scene.visualTierMatrix`, and `scene.fullSettlementSurfaceIdentity`, and strengthened smoke coverage so 90/30/7-day windows assert 48 coverage, all castle/house tiers, six unique roofline/facade/surface families, no trend inputs, no shadows/raycast/transparent materials, stable signatures, and categorized performance accounting. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and screenshot review. Current snapshot: 48 surface identities, 360 logical surface pieces, 6 unique roofline families, 6 unique facade families, surface budget 1,852 triangles / 1 draw call, full-settlement draw calls 369, `other` unchanged at 379 draw calls, total draw calls 2,192, 1,546,202 triangles, no console/asset/WebGL errors.
- Ran a close-up full-settlement surface detail pass with four designers, one graphics optimization agent, and one QA/test agent focused on making nearby walls, windows, eaves, and roof seams read as different topic construction languages without adding draw calls. The reviews converged on expanding the existing baked `fullSettlementSurfaceIdentity` mesh instead of creating a new mesh category: AI circuit seams/cold slit windows, Frontend awning stitches/shop panes, Infra service hatches/bolted vents, Database vault courses/archive slots, Mobile sail ropes/harbor louvers, and Game arena teeth/shield slots. Added roof/facade/window/surface detail families to repos, `speciesSignature`, `topicIdentity[].speciesArchitecture`, `scene.visualTierMatrix`, and `scene.fullSettlementSurfaceIdentity`; strengthened smoke coverage for six unique close-up detail families, all 48 full settlements, all castle/house tiers, no trend inputs, stable 90/30/7-day signatures, and strict merged-mesh accounting. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and screenshot review. Current snapshot: 48 surface identities, 744 close-up logical detail pieces, 6 unique roof/facade/window/surface detail families, close-up addition 6,464 triangles, total surface identity budget 8,316 triangles / 1 draw call, full-settlement draw calls 369, `other` unchanged at 379 draw calls, total draw calls 2,192, 1,559,130 triangles, no console/asset/WebGL errors.
- Ran a directional trend-flow readability pass with three design agents, one data/product agent, one graphics optimization agent, and one QA/test agent focused on making hot fields and top repos understandable from the world geometry without labels. The reviews converged on baking chevron direction ticks into the existing `villageTopRepoFlow` ribbon geometry instead of adding another mesh or material. Added four field-to-top-repo chevron cues per hot field, direction/anchor evidence on `topicIdentity[].trendVisualIdentity`, directional-flow receipt evidence on top repo `worldTrendMarker`s, `fieldTopFlowCoverage`, and `trendReadabilityEvidence`; strengthened smoke coverage so 90/30/7-day windows assert complete directional cue coverage, top-repo anchoring, no non-top repo cue leakage, zero new draw calls, no shadows/raycast targets, and strict trend marker budgets. Verified with `npm run build`, `npm run smoke -- --url http://127.0.0.1:5173`, Playwright payload inspection, and screenshot review. Current snapshot: 6 field-to-top-repo flows, 24 direction cues, 6 top repo directional anchors, cue budget 24 triangles / 0 extra draw calls merged into the 1-draw-call flow ribbon, total trend marker budget 3,904 triangles / 6 draw calls, `other` unchanged at 379 draw calls, total draw calls 2,192, 1,559,178 triangles, no console/asset/WebGL errors.

## TODO

- Review the 158 Building Lab candidates with the user and choose a smaller set of silhouettes to integrate into `src/world.js`.
- Add `GITHUB_TOKEN` or `GH_TOKEN` before running `npm run collect:data` to collect more GitHub event pages per repository.
- Next graphics pass should reduce draw calls by instancing/merging windows, frames, chimneys, market props, road ribbons, and castle merlons before adding more mesh detail.
- For true AAA-style realism, introduce a real asset pipeline: curated GLTF medieval building kits plus PBR texture sets for terrain, stone, roof tile, wood, and cobblestone materials.
- The latest detail pass is intentionally graphics-heavy. Before adding more visible mesh detail, batch facade overlays, recessed windows, doors, merlons, market details, and ground decals into instanced or merged geometry.
- Latest city/structure pass merged internal city road geometry, but castle/window/facade parts are still draw-call heavy. Next optimization should instance or merge castle merlons, window assemblies, gatehouse details, facade overlays, and per-building lot props.
- Next realism pass should prioritize real PBR assets or GLTF kits for close-up building surfaces; the current pass is still procedural geometry and canvas texture based.
- Data now mixes exact public event history for the first few repos per topic with metadata-estimated 90-day histories for the rest, to avoid unauthenticated API rate limits. Add `GITHUB_TOKEN` and raise `GITLAND_EVENT_REPOS_PER_TOPIC` for more exact per-repo 90-day activity.
- Road QA target: keep `spokeRatio = ((roadCount - cityRoadCount) - 8) / repoCount` under 0.2 so villages do not regress into center-spoke patterns.
- Next topic-identity pass: add close-up facade/window/roof texture variation inside the new surface identity contract, preferably by batching or baking so `other` draw calls stay flat.
- Next trend-readability pass: add score-breakdown evidence for why each field's top repo is leading, including normalized activity contributions and rank margin versus the second repo.
- Next graphics optimization pass: batch or instance `scenicWater` props/rings before adding heavier scenery; current water costs 187 draw calls for about 30k triangles, while `other` has little headroom and must not absorb uncategorized additions.
